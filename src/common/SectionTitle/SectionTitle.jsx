import React from "react";
import "./SectionTitle.css";

export const SectionTitle = ({ sectionTitle }) => {
  return (
    <div className="sectionTitleDesign">
      <p>{sectionTitle}</p>
    </div>
  );
};
